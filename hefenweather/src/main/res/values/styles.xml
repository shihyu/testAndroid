<resources>

    <style name="TranslucentTheme" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowTranslucentNavigation">true</item>
        <!--Android 5.x开始需要把颜色设置透明，否则导航栏会呈现系统默认的浅灰色-->
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="apptheme" parent="@android:style/Theme.Material.Light">
        <!-- 状态栏背景 -->
        <item name="android:colorPrimaryDark">@color/default_status_bar_color</item>
        <!-- 标题栏背景 -->
        <item name="android:colorPrimary">@color/default_status_bar_color</item>
    </style>

    <style name="app_list_theme" parent="@style/apptheme">
        <item name="android:listDivider">@drawable/recycle_view_divider</item>
    </style>

    <style name="weather_hour_style">
        <item name="android:layout_marginLeft">5dp</item>
        <item name="android:layout_marginRight">5dp</item>
        <item name="android:layout_marginBottom">1dp</item>
        <item name="android:textColor">@android:color/black</item>
    </style>

    <style name="weather_left_tx_style">
        <item name="android:layout_marginLeft">10dp</item>
        <item name="android:layout_marginTop">15dp</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">17dp</item>
        <item name="android:drawablePadding">10dp</item>
    </style>

    <style name="city_tx_style">
        <item name="android:layout_marginLeft">15dp</item>
        <item name="android:layout_marginTop">10dp</item>
        <item name="android:layout_marginBottom">10dp</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">18dp</item>
    </style>
</resources>
